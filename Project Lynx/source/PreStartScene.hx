package;

import flixel.FlxScene;

class PreStartScene extends FlxScene
{
	override public function start()
	{
		// the scene before the titlescene
		trace("scene testing hehe");
	}

	override public function display(delta:Float)
	{
		// called every frame
	}
}
